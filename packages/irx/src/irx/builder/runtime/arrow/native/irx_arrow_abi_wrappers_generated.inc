// Generated by scripts/gen_arrow_abi.py. Do not edit.

#undef irx_arrow_abi_version
#undef irx_arrow_runtime_has_feature
#undef irx_arrow_status_get_category
#undef irx_arrow_handle_kind_of
#undef irx_arrow_handle_ownership_of
#undef irx_arrow_error_snapshot
#undef irx_arrow_error_code
#undef irx_arrow_error_operation
#undef irx_arrow_error_message
#undef irx_arrow_error_upstream_detail
#undef irx_arrow_error_retain
#undef irx_arrow_error_release
#undef irx_arrow_schema_import_copy
#undef irx_arrow_schema_export
#undef irx_arrow_schema_type_id
#undef irx_arrow_schema_is_nullable
#undef irx_arrow_schema_retain
#undef irx_arrow_schema_release
#undef irx_arrow_array_builder_new
#undef irx_arrow_array_builder_append_null
#undef irx_arrow_array_builder_append_int
#undef irx_arrow_array_builder_append_uint
#undef irx_arrow_array_builder_append_double
#undef irx_arrow_array_builder_int32_new
#undef irx_arrow_array_builder_append_int32
#undef irx_arrow_array_builder_finish
#undef irx_arrow_array_builder_release
#undef irx_arrow_array_length
#undef irx_arrow_array_offset
#undef irx_arrow_array_null_count
#undef irx_arrow_array_type_id
#undef irx_arrow_array_is_nullable
#undef irx_arrow_array_has_validity_bitmap
#undef irx_arrow_array_can_borrow_buffer_view
#undef irx_arrow_array_schema_copy
#undef irx_arrow_array_export
#undef irx_arrow_array_import
#undef irx_arrow_array_import_copy
#undef irx_arrow_array_import_move
#undef irx_arrow_array_validity_bitmap
#undef irx_arrow_array_borrow_buffer_view
#undef irx_arrow_array_retain
#undef irx_arrow_array_release
#undef irx_arrow_record_batch_import_move
#undef irx_arrow_record_batch_export
#undef irx_arrow_record_batch_num_rows
#undef irx_arrow_record_batch_num_columns
#undef irx_arrow_record_batch_retain
#undef irx_arrow_record_batch_release
#undef irx_arrow_tensor_builder_new
#undef irx_arrow_tensor_builder_append_int
#undef irx_arrow_tensor_builder_append_uint
#undef irx_arrow_tensor_builder_append_double
#undef irx_arrow_tensor_builder_finish
#undef irx_arrow_tensor_builder_release
#undef irx_arrow_tensor_type_id
#undef irx_arrow_tensor_ndim
#undef irx_arrow_tensor_size
#undef irx_arrow_tensor_shape
#undef irx_arrow_tensor_strides
#undef irx_arrow_tensor_borrow_buffer_view
#undef irx_arrow_tensor_retain
#undef irx_arrow_tensor_release
#undef irx_arrow_table_new_from_arrays
#undef irx_arrow_table_num_rows
#undef irx_arrow_table_num_columns
#undef irx_arrow_table_column_by_name
#undef irx_arrow_table_column_by_index
#undef irx_arrow_table_retain
#undef irx_arrow_table_release
#undef irx_arrow_chunked_array_retain
#undef irx_arrow_chunked_array_release
#undef irx_arrow_tensor_release_callback
#undef irx_arrow_last_error
#undef irx_arrow_type_import_copy
#undef irx_arrow_type_export
#undef irx_arrow_type_retain
#undef irx_arrow_type_release
#undef irx_arrow_field_import_copy
#undef irx_arrow_field_export
#undef irx_arrow_field_retain
#undef irx_arrow_field_release
#undef irx_arrow_type_equals
#undef irx_arrow_field_equals
#undef irx_arrow_schema_equals
#undef irx_arrow_type_num_fields
#undef irx_arrow_type_field
#undef irx_arrow_schema_num_fields
#undef irx_arrow_schema_field
#undef irx_arrow_field_type
#undef irx_arrow_field_nullable
#undef irx_arrow_type_bit_width
#undef irx_arrow_field_name_copy
#undef irx_arrow_array_get_int
#undef irx_arrow_array_get_uint
#undef irx_arrow_array_get_double
#undef irx_arrow_array_slice
#undef irx_arrow_array_concat
#undef irx_arrow_array_copy
#undef irx_arrow_array_equal
#undef irx_arrow_array_builder_finish_typed
#undef irx_arrow_array_builder_reserve
#undef irx_arrow_array_builder_length
#undef irx_arrow_array_builder_build
#undef irx_arrow_chunked_new
#undef irx_arrow_chunked_length
#undef irx_arrow_chunked_null_count
#undef irx_arrow_chunked_num_chunks
#undef irx_arrow_chunked_get_int
#undef irx_arrow_chunked_get_uint
#undef irx_arrow_chunked_get_double
#undef irx_arrow_chunked_chunk
#undef irx_arrow_chunked_combine
#undef irx_arrow_chunked_slice
#undef irx_arrow_chunked_copy
#undef irx_arrow_chunked_concat
#undef irx_arrow_chunked_equal
#undef irx_arrow_batch_new_typed
#undef irx_arrow_batch_rows
#undef irx_arrow_batch_columns
#undef irx_arrow_batch_schema
#undef irx_arrow_batch_column_checked
#undef irx_arrow_batch_slice
#undef irx_arrow_batch_select
#undef irx_arrow_batch_with_schema
#undef irx_arrow_batch_set_column
#undef irx_arrow_batch_remove
#undef irx_arrow_table_new_typed
#undef irx_arrow_table_rows
#undef irx_arrow_table_columns
#undef irx_arrow_table_schema
#undef irx_arrow_table_column_checked
#undef irx_arrow_table_slice
#undef irx_arrow_table_select
#undef irx_arrow_table_with_schema
#undef irx_arrow_table_set_column
#undef irx_arrow_table_remove
#undef irx_arrow_batch_to_table
#undef irx_arrow_table_to_batch
#undef irx_arrow_table_combine

#if defined(__GNUC__) || defined(__clang__)
#pragma GCC visibility pop
#endif

static irx_arrow_status finish_generated_call(
    irx_arrow_status status,
    const char* operation,
    irx_arrow_error_handle** out_failure) {
  if (status == IRX_ARROW_STATUS_OK ||
      status == IRX_ARROW_STATUS_END_OF_STREAM) {
    return status;
  }
  copy_error_text(
      current_error.operation,
      sizeof(current_error.operation),
      operation);
  const irx_arrow_status snapshot_status =
      irx_arrow_internal_error_snapshot(out_failure);
  if (snapshot_status != IRX_ARROW_STATUS_OK) {
    *out_failure = nullptr;
  }
  return status;
}

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
uint32_t irx_arrow_abi_version(void) {
  return irx_arrow_internal_abi_version();
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_runtime_has_feature(
    irx_arrow_runtime_feature_id feature_id,
    uint32_t required_contract_version,
    int32_t* out_available,
    uint32_t* out_supported_contract_version,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_runtime_has_feature");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_runtime_has_feature(
          feature_id,
          required_contract_version,
          out_available,
          out_supported_contract_version);
  return finish_generated_call(
      status,
      "irx_arrow_runtime_has_feature",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status_category irx_arrow_status_get_category(
    irx_arrow_status status) {
  return irx_arrow_internal_status_get_category(status);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_handle_kind_of(
    const void* handle,
    irx_arrow_handle_kind* out_kind,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_handle_kind_of");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_handle_kind_of(handle, out_kind);
  return finish_generated_call(
      status,
      "irx_arrow_handle_kind_of",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_handle_ownership_of(
    const void* handle,
    irx_arrow_handle_ownership* out_ownership,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_handle_ownership_of");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_handle_ownership_of(handle, out_ownership);
  return finish_generated_call(
      status,
      "irx_arrow_handle_ownership_of",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_error_snapshot(
    irx_arrow_error_handle** out_error,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_error_snapshot");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_error_snapshot(out_error);
  return finish_generated_call(
      status,
      "irx_arrow_error_snapshot",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_error_code(
    const irx_arrow_error_handle* error,
    irx_arrow_status* out_code,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_error_code");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_code == nullptr) {
    begin_operation("irx_arrow_error_code");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_code must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_error_code",
        out_failure);
  }
  *out_code = 0;
  irx_arrow_status value =
      irx_arrow_internal_error_code(error);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_code = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_error_code",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_error_operation(
    const irx_arrow_error_handle* error,
    const char** out_operation,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_error_operation");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_operation == nullptr) {
    begin_operation("irx_arrow_error_operation");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_operation must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_error_operation",
        out_failure);
  }
  *out_operation = nullptr;
  const char* value =
      irx_arrow_internal_error_operation(error);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_operation = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_error_operation",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_error_message(
    const irx_arrow_error_handle* error,
    const char** out_message,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_error_message");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_message == nullptr) {
    begin_operation("irx_arrow_error_message");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_message must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_error_message",
        out_failure);
  }
  *out_message = nullptr;
  const char* value =
      irx_arrow_internal_error_message(error);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_message = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_error_message",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_error_upstream_detail(
    const irx_arrow_error_handle* error,
    const char** out_upstream_detail,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_error_upstream_detail");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_upstream_detail == nullptr) {
    begin_operation("irx_arrow_error_upstream_detail");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_upstream_detail must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_error_upstream_detail",
        out_failure);
  }
  *out_upstream_detail = nullptr;
  const char* value =
      irx_arrow_internal_error_upstream_detail(error);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_upstream_detail = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_error_upstream_detail",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_error_retain(
    const irx_arrow_error_handle* error,
    irx_arrow_error_handle** out_error,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_error_retain");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_error_retain(error, out_error);
  return finish_generated_call(
      status,
      "irx_arrow_error_retain",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
irx_arrow_status irx_arrow_error_release(
    irx_arrow_error_handle** error,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_error_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_error_release(error);
  return finish_generated_call(
      status,
      "irx_arrow_error_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_schema_import_copy(
    const struct ArrowSchema* schema,
    irx_arrow_schema_handle** out_schema,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_schema_import_copy");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_schema_import_copy(schema, out_schema);
  return finish_generated_call(
      status,
      "irx_arrow_schema_import_copy",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_schema_export(
    const irx_arrow_schema_handle* schema,
    struct ArrowSchema* out_schema,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_schema_export");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_schema_export(schema, out_schema);
  return finish_generated_call(
      status,
      "irx_arrow_schema_export",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_schema_type_id(
    const irx_arrow_schema_handle* schema,
    int32_t* out_type_id,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_schema_type_id");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_type_id == nullptr) {
    begin_operation("irx_arrow_schema_type_id");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_type_id must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_schema_type_id",
        out_failure);
  }
  *out_type_id = 0;
  int32_t value =
      irx_arrow_internal_schema_type_id(schema);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_type_id = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_schema_type_id",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_schema_is_nullable(
    const irx_arrow_schema_handle* schema,
    int32_t* out_is_nullable,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_schema_is_nullable");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_is_nullable == nullptr) {
    begin_operation("irx_arrow_schema_is_nullable");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_is_nullable must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_schema_is_nullable",
        out_failure);
  }
  *out_is_nullable = 0;
  int32_t value =
      irx_arrow_internal_schema_is_nullable(schema);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_is_nullable = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_schema_is_nullable",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_schema_retain(
    const irx_arrow_schema_handle* schema,
    irx_arrow_schema_handle** out_schema,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_schema_retain");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_schema_retain(schema, out_schema);
  return finish_generated_call(
      status,
      "irx_arrow_schema_retain",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_schema_release(
    irx_arrow_schema_handle** schema,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_schema_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_schema_release(schema);
  return finish_generated_call(
      status,
      "irx_arrow_schema_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_new(
    int32_t type_id,
    irx_arrow_array_builder_handle** out_builder,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_new");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_new(type_id, out_builder);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_new",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_append_null(
    irx_arrow_array_builder_handle* builder,
    int64_t count,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_append_null");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_append_null(builder, count);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_append_null",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_append_int(
    irx_arrow_array_builder_handle* builder,
    int64_t value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_append_int");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_append_int(builder, value);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_append_int",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_append_uint(
    irx_arrow_array_builder_handle* builder,
    uint64_t value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_append_uint");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_append_uint(builder, value);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_append_uint",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_append_double(
    irx_arrow_array_builder_handle* builder,
    double value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_append_double");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_append_double(builder, value);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_append_double",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_int32_new(
    irx_arrow_array_builder_handle** out_builder,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_int32_new");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_int32_new(out_builder);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_int32_new",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_append_int32(
    irx_arrow_array_builder_handle* builder,
    int32_t value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_append_int32");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_append_int32(builder, value);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_append_int32",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_finish(
    irx_arrow_array_builder_handle** builder,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_finish");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_finish(builder, out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_finish",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_release(
    irx_arrow_array_builder_handle** builder,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_release(builder);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_length(
    const irx_arrow_array_handle* array,
    int64_t* out_length,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_length");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_length == nullptr) {
    begin_operation("irx_arrow_array_length");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_length must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_array_length",
        out_failure);
  }
  *out_length = 0;
  int64_t value =
      irx_arrow_internal_array_length(array);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_length = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_array_length",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_offset(
    const irx_arrow_array_handle* array,
    int64_t* out_offset,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_offset");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_offset == nullptr) {
    begin_operation("irx_arrow_array_offset");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_offset must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_array_offset",
        out_failure);
  }
  *out_offset = 0;
  int64_t value =
      irx_arrow_internal_array_offset(array);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_offset = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_array_offset",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_null_count(
    const irx_arrow_array_handle* array,
    int64_t* out_null_count,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_null_count");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_null_count == nullptr) {
    begin_operation("irx_arrow_array_null_count");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_null_count must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_array_null_count",
        out_failure);
  }
  *out_null_count = 0;
  int64_t value =
      irx_arrow_internal_array_null_count(array);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_null_count = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_array_null_count",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_type_id(
    const irx_arrow_array_handle* array,
    int32_t* out_type_id,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_type_id");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_type_id == nullptr) {
    begin_operation("irx_arrow_array_type_id");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_type_id must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_array_type_id",
        out_failure);
  }
  *out_type_id = 0;
  int32_t value =
      irx_arrow_internal_array_type_id(array);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_type_id = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_array_type_id",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_is_nullable(
    const irx_arrow_array_handle* array,
    int32_t* out_is_nullable,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_is_nullable");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_is_nullable == nullptr) {
    begin_operation("irx_arrow_array_is_nullable");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_is_nullable must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_array_is_nullable",
        out_failure);
  }
  *out_is_nullable = 0;
  int32_t value =
      irx_arrow_internal_array_is_nullable(array);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_is_nullable = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_array_is_nullable",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_has_validity_bitmap(
    const irx_arrow_array_handle* array,
    int32_t* out_has_validity_bitmap,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_has_validity_bitmap");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_has_validity_bitmap == nullptr) {
    begin_operation("irx_arrow_array_has_validity_bitmap");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_has_validity_bitmap must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_array_has_validity_bitmap",
        out_failure);
  }
  *out_has_validity_bitmap = 0;
  int32_t value =
      irx_arrow_internal_array_has_validity_bitmap(array);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_has_validity_bitmap = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_array_has_validity_bitmap",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_can_borrow_buffer_view(
    const irx_arrow_array_handle* array,
    int32_t* out_can_borrow,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_can_borrow_buffer_view");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_can_borrow == nullptr) {
    begin_operation("irx_arrow_array_can_borrow_buffer_view");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_can_borrow must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_array_can_borrow_buffer_view",
        out_failure);
  }
  *out_can_borrow = 0;
  int32_t value =
      irx_arrow_internal_array_can_borrow_buffer_view(array);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_can_borrow = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_array_can_borrow_buffer_view",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_schema_copy(
    const irx_arrow_array_handle* array,
    irx_arrow_schema_handle** out_schema,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_schema_copy");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_schema_copy(array, out_schema);
  return finish_generated_call(
      status,
      "irx_arrow_array_schema_copy",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_export(
    const irx_arrow_array_handle* array,
    struct ArrowArray* out_array,
    struct ArrowSchema* out_schema,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_export");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_export(array, out_array, out_schema);
  return finish_generated_call(
      status,
      "irx_arrow_array_export",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_import(
    const struct ArrowArray* array,
    const struct ArrowSchema* schema,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_import");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_import(array, schema, out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_import",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_import_copy(
    const struct ArrowArray* array,
    const struct ArrowSchema* schema,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_import_copy");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_import_copy(array, schema, out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_import_copy",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_import_move(
    struct ArrowArray* array,
    struct ArrowSchema* schema,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_import_move");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_import_move(array, schema, out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_import_move",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_validity_bitmap(
    const irx_arrow_array_handle* array,
    const void** out_data,
    int64_t* out_offset_bits,
    int64_t* out_length_bits,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_validity_bitmap");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_validity_bitmap(
          array,
          out_data,
          out_offset_bits,
          out_length_bits);
  return finish_generated_call(
      status,
      "irx_arrow_array_validity_bitmap",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_borrow_buffer_view(
    const irx_arrow_array_handle* array,
    irx_buffer_view* out_view,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_borrow_buffer_view");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_borrow_buffer_view(array, out_view);
  return finish_generated_call(
      status,
      "irx_arrow_array_borrow_buffer_view",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_retain(
    const irx_arrow_array_handle* array,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_retain");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_retain(array, out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_retain",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_release(
    irx_arrow_array_handle** array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_release(array);
  return finish_generated_call(
      status,
      "irx_arrow_array_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_RECORD_BATCH)
irx_arrow_status irx_arrow_record_batch_import_move(
    struct ArrowArray* array,
    struct ArrowSchema* schema,
    irx_arrow_record_batch_handle** out_batch,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_record_batch_import_move");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_record_batch_import_move(array, schema, out_batch);
  return finish_generated_call(
      status,
      "irx_arrow_record_batch_import_move",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_RECORD_BATCH)
irx_arrow_status irx_arrow_record_batch_export(
    const irx_arrow_record_batch_handle* batch,
    struct ArrowArray* out_array,
    struct ArrowSchema* out_schema,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_record_batch_export");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_record_batch_export(batch, out_array, out_schema);
  return finish_generated_call(
      status,
      "irx_arrow_record_batch_export",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_RECORD_BATCH)
irx_arrow_status irx_arrow_record_batch_num_rows(
    const irx_arrow_record_batch_handle* batch,
    int64_t* out_num_rows,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_record_batch_num_rows");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_num_rows == nullptr) {
    begin_operation("irx_arrow_record_batch_num_rows");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_num_rows must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_record_batch_num_rows",
        out_failure);
  }
  *out_num_rows = 0;
  int64_t value =
      irx_arrow_internal_record_batch_num_rows(batch);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_num_rows = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_record_batch_num_rows",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_RECORD_BATCH)
irx_arrow_status irx_arrow_record_batch_num_columns(
    const irx_arrow_record_batch_handle* batch,
    int64_t* out_num_columns,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_record_batch_num_columns");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_num_columns == nullptr) {
    begin_operation("irx_arrow_record_batch_num_columns");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_num_columns must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_record_batch_num_columns",
        out_failure);
  }
  *out_num_columns = 0;
  int64_t value =
      irx_arrow_internal_record_batch_num_columns(batch);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_num_columns = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_record_batch_num_columns",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_RECORD_BATCH)
irx_arrow_status irx_arrow_record_batch_retain(
    const irx_arrow_record_batch_handle* batch,
    irx_arrow_record_batch_handle** out_batch,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_record_batch_retain");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_record_batch_retain(batch, out_batch);
  return finish_generated_call(
      status,
      "irx_arrow_record_batch_retain",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_RECORD_BATCH)
irx_arrow_status irx_arrow_record_batch_release(
    irx_arrow_record_batch_handle** batch,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_record_batch_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_record_batch_release(batch);
  return finish_generated_call(
      status,
      "irx_arrow_record_batch_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_builder_new(
    int32_t type_id,
    int32_t ndim,
    const int64_t* shape,
    const int64_t* strides,
    irx_arrow_tensor_builder_handle** out_builder,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_builder_new");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_tensor_builder_new(
          type_id,
          ndim,
          shape,
          strides,
          out_builder);
  return finish_generated_call(
      status,
      "irx_arrow_tensor_builder_new",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_builder_append_int(
    irx_arrow_tensor_builder_handle* builder,
    int64_t value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_builder_append_int");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_tensor_builder_append_int(builder, value);
  return finish_generated_call(
      status,
      "irx_arrow_tensor_builder_append_int",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_builder_append_uint(
    irx_arrow_tensor_builder_handle* builder,
    uint64_t value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_builder_append_uint");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_tensor_builder_append_uint(builder, value);
  return finish_generated_call(
      status,
      "irx_arrow_tensor_builder_append_uint",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_builder_append_double(
    irx_arrow_tensor_builder_handle* builder,
    double value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_builder_append_double");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_tensor_builder_append_double(builder, value);
  return finish_generated_call(
      status,
      "irx_arrow_tensor_builder_append_double",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_builder_finish(
    irx_arrow_tensor_builder_handle** builder,
    irx_arrow_tensor_handle** out_tensor,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_builder_finish");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_tensor_builder_finish(builder, out_tensor);
  return finish_generated_call(
      status,
      "irx_arrow_tensor_builder_finish",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_builder_release(
    irx_arrow_tensor_builder_handle** builder,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_builder_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_tensor_builder_release(builder);
  return finish_generated_call(
      status,
      "irx_arrow_tensor_builder_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_type_id(
    const irx_arrow_tensor_handle* tensor,
    int32_t* out_type_id,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_type_id");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_type_id == nullptr) {
    begin_operation("irx_arrow_tensor_type_id");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_type_id must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_tensor_type_id",
        out_failure);
  }
  *out_type_id = 0;
  int32_t value =
      irx_arrow_internal_tensor_type_id(tensor);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_type_id = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_tensor_type_id",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_ndim(
    const irx_arrow_tensor_handle* tensor,
    int32_t* out_ndim,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_ndim");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_ndim == nullptr) {
    begin_operation("irx_arrow_tensor_ndim");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_ndim must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_tensor_ndim",
        out_failure);
  }
  *out_ndim = 0;
  int32_t value =
      irx_arrow_internal_tensor_ndim(tensor);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_ndim = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_tensor_ndim",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_size(
    const irx_arrow_tensor_handle* tensor,
    int64_t* out_size,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_size");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_size == nullptr) {
    begin_operation("irx_arrow_tensor_size");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_size must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_tensor_size",
        out_failure);
  }
  *out_size = 0;
  int64_t value =
      irx_arrow_internal_tensor_size(tensor);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_size = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_tensor_size",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_shape(
    const irx_arrow_tensor_handle* tensor,
    const int64_t** out_shape,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_shape");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_shape == nullptr) {
    begin_operation("irx_arrow_tensor_shape");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_shape must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_tensor_shape",
        out_failure);
  }
  *out_shape = nullptr;
  const int64_t* value =
      irx_arrow_internal_tensor_shape(tensor);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_shape = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_tensor_shape",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_strides(
    const irx_arrow_tensor_handle* tensor,
    const int64_t** out_strides,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_strides");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_strides == nullptr) {
    begin_operation("irx_arrow_tensor_strides");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_strides must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_tensor_strides",
        out_failure);
  }
  *out_strides = nullptr;
  const int64_t* value =
      irx_arrow_internal_tensor_strides(tensor);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_strides = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_tensor_strides",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_borrow_buffer_view(
    const irx_arrow_tensor_handle* tensor,
    irx_buffer_view* out_view,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_borrow_buffer_view");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_tensor_borrow_buffer_view(tensor, out_view);
  return finish_generated_call(
      status,
      "irx_arrow_tensor_borrow_buffer_view",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_retain(
    const irx_arrow_tensor_handle* tensor,
    irx_arrow_tensor_handle** out_tensor,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_retain");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_tensor_retain(tensor, out_tensor);
  return finish_generated_call(
      status,
      "irx_arrow_tensor_retain",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
irx_arrow_status irx_arrow_tensor_release(
    irx_arrow_tensor_handle** tensor,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_tensor_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_tensor_release(tensor);
  return finish_generated_call(
      status,
      "irx_arrow_tensor_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_new_from_arrays(
    int64_t column_count,
    const char** names,
    irx_arrow_array_handle** arrays,
    irx_arrow_table_handle** out_table,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_new_from_arrays");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_new_from_arrays(
          column_count,
          names,
          arrays,
          out_table);
  return finish_generated_call(
      status,
      "irx_arrow_table_new_from_arrays",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_num_rows(
    const irx_arrow_table_handle* table,
    int64_t* out_num_rows,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_num_rows");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_num_rows == nullptr) {
    begin_operation("irx_arrow_table_num_rows");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_num_rows must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_table_num_rows",
        out_failure);
  }
  *out_num_rows = 0;
  int64_t value =
      irx_arrow_internal_table_num_rows(table);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_num_rows = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_table_num_rows",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_num_columns(
    const irx_arrow_table_handle* table,
    int64_t* out_num_columns,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_num_columns");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  if (out_num_columns == nullptr) {
    begin_operation("irx_arrow_table_num_columns");
    const irx_arrow_status status = set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_num_columns must not be NULL");
    return finish_generated_call(
        status,
        "irx_arrow_table_num_columns",
        out_failure);
  }
  *out_num_columns = 0;
  int64_t value =
      irx_arrow_internal_table_num_columns(table);
  const irx_arrow_status status = current_error.code;
  if (status == IRX_ARROW_STATUS_OK) {
    *out_num_columns = value;
  }
  return finish_generated_call(
      status,
      "irx_arrow_table_num_columns",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_column_by_name(
    const irx_arrow_table_handle* table,
    const char* name,
    irx_arrow_chunked_array_handle** out_column,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_column_by_name");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_column_by_name(table, name, out_column);
  return finish_generated_call(
      status,
      "irx_arrow_table_column_by_name",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_column_by_index(
    const irx_arrow_table_handle* table,
    int32_t index,
    irx_arrow_chunked_array_handle** out_column,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_column_by_index");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_column_by_index(table, index, out_column);
  return finish_generated_call(
      status,
      "irx_arrow_table_column_by_index",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_retain(
    const irx_arrow_table_handle* table,
    irx_arrow_table_handle** out_table,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_retain");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_retain(table, out_table);
  return finish_generated_call(
      status,
      "irx_arrow_table_retain",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_release(
    irx_arrow_table_handle** table,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_release(table);
  return finish_generated_call(
      status,
      "irx_arrow_table_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_chunked_array_retain(
    const irx_arrow_chunked_array_handle* column,
    irx_arrow_chunked_array_handle** out_column,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_array_retain");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_array_retain(column, out_column);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_array_retain",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_chunked_array_release(
    irx_arrow_chunked_array_handle** column,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_array_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_array_release(column);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_array_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_TENSOR)
void irx_arrow_tensor_release_callback(
    void* tensor) {
  irx_arrow_internal_tensor_release_callback(tensor);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_CORE)
const char* irx_arrow_last_error(void) {
  return irx_arrow_internal_last_error();
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_type_import_copy(
    const struct ArrowSchema* schema,
    irx_arrow_type_handle** out_value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_type_import_copy");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_type_import_copy(schema, out_value);
  return finish_generated_call(
      status,
      "irx_arrow_type_import_copy",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_type_export(
    const irx_arrow_type_handle* value,
    struct ArrowSchema* out_schema,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_type_export");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_type_export(value, out_schema);
  return finish_generated_call(
      status,
      "irx_arrow_type_export",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_type_retain(
    const irx_arrow_type_handle* value,
    irx_arrow_type_handle** out_value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_type_retain");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_type_retain(value, out_value);
  return finish_generated_call(
      status,
      "irx_arrow_type_retain",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_type_release(
    irx_arrow_type_handle** value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_type_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_type_release(value);
  return finish_generated_call(
      status,
      "irx_arrow_type_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_field_import_copy(
    const struct ArrowSchema* schema,
    irx_arrow_field_handle** out_value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_field_import_copy");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_field_import_copy(schema, out_value);
  return finish_generated_call(
      status,
      "irx_arrow_field_import_copy",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_field_export(
    const irx_arrow_field_handle* value,
    struct ArrowSchema* out_schema,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_field_export");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_field_export(value, out_schema);
  return finish_generated_call(
      status,
      "irx_arrow_field_export",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_field_retain(
    const irx_arrow_field_handle* value,
    irx_arrow_field_handle** out_value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_field_retain");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_field_retain(value, out_value);
  return finish_generated_call(
      status,
      "irx_arrow_field_retain",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_field_release(
    irx_arrow_field_handle** value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_field_release");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_field_release(value);
  return finish_generated_call(
      status,
      "irx_arrow_field_release",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_type_equals(
    const irx_arrow_type_handle* left,
    const irx_arrow_type_handle* right,
    int32_t* out_equal,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_type_equals");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_type_equals(left, right, out_equal);
  return finish_generated_call(
      status,
      "irx_arrow_type_equals",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_field_equals(
    const irx_arrow_field_handle* left,
    const irx_arrow_field_handle* right,
    int32_t* out_equal,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_field_equals");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_field_equals(left, right, out_equal);
  return finish_generated_call(
      status,
      "irx_arrow_field_equals",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_schema_equals(
    const irx_arrow_schema_handle* left,
    const irx_arrow_schema_handle* right,
    int32_t* out_equal,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_schema_equals");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_schema_equals(left, right, out_equal);
  return finish_generated_call(
      status,
      "irx_arrow_schema_equals",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_type_num_fields(
    const irx_arrow_type_handle* value,
    int64_t* out_count,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_type_num_fields");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_type_num_fields(value, out_count);
  return finish_generated_call(
      status,
      "irx_arrow_type_num_fields",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_type_field(
    const irx_arrow_type_handle* value,
    int64_t index,
    irx_arrow_field_handle** out_field,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_type_field");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_type_field(value, index, out_field);
  return finish_generated_call(
      status,
      "irx_arrow_type_field",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_schema_num_fields(
    const irx_arrow_schema_handle* value,
    int64_t* out_count,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_schema_num_fields");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_schema_num_fields(value, out_count);
  return finish_generated_call(
      status,
      "irx_arrow_schema_num_fields",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_schema_field(
    const irx_arrow_schema_handle* value,
    int64_t index,
    irx_arrow_field_handle** out_field,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_schema_field");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_schema_field(value, index, out_field);
  return finish_generated_call(
      status,
      "irx_arrow_schema_field",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_field_type(
    const irx_arrow_field_handle* value,
    irx_arrow_type_handle** out_type,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_field_type");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_field_type(value, out_type);
  return finish_generated_call(
      status,
      "irx_arrow_field_type",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_field_nullable(
    const irx_arrow_field_handle* value,
    int32_t* out_nullable,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_field_nullable");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_field_nullable(value, out_nullable);
  return finish_generated_call(
      status,
      "irx_arrow_field_nullable",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_type_bit_width(
    const irx_arrow_type_handle* value,
    int64_t* out_width,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_type_bit_width");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_type_bit_width(value, out_width);
  return finish_generated_call(
      status,
      "irx_arrow_type_bit_width",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_field_name_copy(
    const irx_arrow_field_handle* value,
    const char** out_name,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_field_name_copy");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_field_name_copy(value, out_name);
  return finish_generated_call(
      status,
      "irx_arrow_field_name_copy",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_get_int(
    const irx_arrow_array_handle* array,
    int64_t index,
    int32_t* out_valid,
    int64_t* out_value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_get_int");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_get_int(array, index, out_valid, out_value);
  return finish_generated_call(
      status,
      "irx_arrow_array_get_int",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_get_uint(
    const irx_arrow_array_handle* array,
    int64_t index,
    int32_t* out_valid,
    uint64_t* out_value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_get_uint");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_get_uint(array, index, out_valid, out_value);
  return finish_generated_call(
      status,
      "irx_arrow_array_get_uint",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_get_double(
    const irx_arrow_array_handle* array,
    int64_t index,
    int32_t* out_valid,
    double* out_value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_get_double");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_get_double(array, index, out_valid, out_value);
  return finish_generated_call(
      status,
      "irx_arrow_array_get_double",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_slice(
    const irx_arrow_array_handle* array,
    int64_t offset,
    int64_t length,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_slice");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_slice(array, offset, length, out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_slice",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_concat(
    const irx_arrow_array_handle* lhs,
    const irx_arrow_array_handle* rhs,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_concat");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_concat(lhs, rhs, out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_concat",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_copy(
    const irx_arrow_array_handle* array,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_copy");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_copy(array, out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_copy",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_equal(
    const irx_arrow_array_handle* lhs,
    const irx_arrow_array_handle* rhs,
    int32_t* out_equal,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_equal");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_equal(lhs, rhs, out_equal);
  return finish_generated_call(
      status,
      "irx_arrow_array_equal",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_finish_typed(
    irx_arrow_array_builder_handle** builder,
    int32_t nullable,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_finish_typed");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_finish_typed(
          builder,
          nullable,
          out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_finish_typed",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_reserve(
    irx_arrow_array_builder_handle* builder,
    int64_t additional,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_reserve");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_reserve(builder, additional);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_reserve",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_length(
    const irx_arrow_array_builder_handle* builder,
    int64_t* out_length,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_length");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_length(builder, out_length);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_length",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_array_builder_build(
    irx_arrow_array_builder_handle* builder,
    int32_t nullable,
    irx_arrow_array_handle** out_array,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_array_builder_build");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_array_builder_build(builder, nullable, out_array);
  return finish_generated_call(
      status,
      "irx_arrow_array_builder_build",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_new(
    int32_t type_id,
    int32_t nullable,
    const void** chunks,
    int64_t count,
    irx_arrow_chunked_array_handle** out_value,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_new");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_new(
          type_id,
          nullable,
          chunks,
          count,
          out_value);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_new",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_length(
    const irx_arrow_chunked_array_handle* value,
    int64_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_length");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_length(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_length",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_null_count(
    const irx_arrow_chunked_array_handle* value,
    int64_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_null_count");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_null_count(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_null_count",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_num_chunks(
    const irx_arrow_chunked_array_handle* value,
    int64_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_num_chunks");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_num_chunks(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_num_chunks",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_get_int(
    const irx_arrow_chunked_array_handle* value,
    int64_t index,
    int32_t* valid,
    int64_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_get_int");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_get_int(value, index, valid, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_get_int",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_get_uint(
    const irx_arrow_chunked_array_handle* value,
    int64_t index,
    int32_t* valid,
    uint64_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_get_uint");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_get_uint(value, index, valid, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_get_uint",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_get_double(
    const irx_arrow_chunked_array_handle* value,
    int64_t index,
    int32_t* valid,
    double* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_get_double");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_get_double(value, index, valid, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_get_double",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_chunk(
    const irx_arrow_chunked_array_handle* value,
    int64_t index,
    irx_arrow_array_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_chunk");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_chunk(value, index, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_chunk",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_combine(
    const irx_arrow_chunked_array_handle* value,
    irx_arrow_array_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_combine");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_combine(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_combine",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_slice(
    const irx_arrow_chunked_array_handle* value,
    int64_t offset,
    int64_t length,
    irx_arrow_chunked_array_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_slice");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_slice(value, offset, length, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_slice",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_copy(
    const irx_arrow_chunked_array_handle* value,
    irx_arrow_chunked_array_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_copy");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_copy(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_copy",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_concat(
    const irx_arrow_chunked_array_handle* lhs,
    const irx_arrow_chunked_array_handle* rhs,
    irx_arrow_chunked_array_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_concat");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_concat(lhs, rhs, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_concat",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_ARRAY)
irx_arrow_status irx_arrow_chunked_equal(
    const irx_arrow_chunked_array_handle* lhs,
    const irx_arrow_chunked_array_handle* rhs,
    int32_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_chunked_equal");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_chunked_equal(lhs, rhs, output);
  return finish_generated_call(
      status,
      "irx_arrow_chunked_equal",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_new_typed(
    const irx_arrow_schema_handle* schema,
    const void** columns,
    int64_t count,
    int64_t rows,
    irx_arrow_record_batch_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_new_typed");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_new_typed(schema, columns, count, rows, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_new_typed",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_rows(
    const irx_arrow_record_batch_handle* value,
    int64_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_rows");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_rows(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_rows",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_columns(
    const irx_arrow_record_batch_handle* value,
    int64_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_columns");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_columns(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_columns",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_schema(
    const irx_arrow_record_batch_handle* value,
    irx_arrow_schema_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_schema");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_schema(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_schema",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_column_checked(
    const irx_arrow_record_batch_handle* value,
    int64_t index,
    const irx_arrow_field_handle* expected,
    irx_arrow_array_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_column_checked");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_column_checked(value, index, expected, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_column_checked",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_slice(
    const irx_arrow_record_batch_handle* value,
    int64_t offset,
    int64_t length,
    irx_arrow_record_batch_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_slice");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_slice(value, offset, length, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_slice",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_select(
    const irx_arrow_record_batch_handle* value,
    const int64_t* indices,
    int64_t count,
    irx_arrow_record_batch_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_select");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_select(value, indices, count, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_select",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_with_schema(
    const irx_arrow_record_batch_handle* value,
    const irx_arrow_schema_handle* schema,
    irx_arrow_record_batch_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_with_schema");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_with_schema(value, schema, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_with_schema",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_set_column(
    const irx_arrow_record_batch_handle* value,
    int64_t index,
    const irx_arrow_field_handle* field,
    const irx_arrow_array_handle* column,
    irx_arrow_record_batch_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_set_column");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_set_column(value, index, field, column, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_set_column",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_remove(
    const irx_arrow_record_batch_handle* value,
    int64_t index,
    irx_arrow_record_batch_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_remove");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_remove(value, index, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_remove",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_new_typed(
    const irx_arrow_schema_handle* schema,
    const void** columns,
    int64_t count,
    int64_t rows,
    irx_arrow_table_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_new_typed");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_new_typed(schema, columns, count, rows, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_new_typed",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_rows(
    const irx_arrow_table_handle* value,
    int64_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_rows");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_rows(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_rows",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_columns(
    const irx_arrow_table_handle* value,
    int64_t* output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_columns");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_columns(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_columns",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_schema(
    const irx_arrow_table_handle* value,
    irx_arrow_schema_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_schema");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_schema(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_schema",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_column_checked(
    const irx_arrow_table_handle* value,
    int64_t index,
    const irx_arrow_field_handle* expected,
    irx_arrow_chunked_array_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_column_checked");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_column_checked(value, index, expected, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_column_checked",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_slice(
    const irx_arrow_table_handle* value,
    int64_t offset,
    int64_t length,
    irx_arrow_table_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_slice");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_slice(value, offset, length, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_slice",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_select(
    const irx_arrow_table_handle* value,
    const int64_t* indices,
    int64_t count,
    irx_arrow_table_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_select");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_select(value, indices, count, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_select",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_with_schema(
    const irx_arrow_table_handle* value,
    const irx_arrow_schema_handle* schema,
    irx_arrow_table_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_with_schema");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_with_schema(value, schema, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_with_schema",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_set_column(
    const irx_arrow_table_handle* value,
    int64_t index,
    const irx_arrow_field_handle* field,
    const irx_arrow_chunked_array_handle* column,
    irx_arrow_table_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_set_column");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_set_column(value, index, field, column, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_set_column",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_remove(
    const irx_arrow_table_handle* value,
    int64_t index,
    irx_arrow_table_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_remove");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_remove(value, index, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_remove",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_batch_to_table(
    const irx_arrow_record_batch_handle* value,
    irx_arrow_table_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_batch_to_table");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_batch_to_table(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_batch_to_table",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_to_batch(
    const irx_arrow_table_handle* value,
    irx_arrow_record_batch_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_to_batch");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_to_batch(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_to_batch",
      out_failure);
}

#endif

#if defined(IRX_ARROW_RUNTIME_BUILD_DATAFRAME)
irx_arrow_status irx_arrow_table_combine(
    const irx_arrow_table_handle* value,
    irx_arrow_table_handle** output,
    irx_arrow_error_handle** out_failure) {
  if (out_failure == nullptr) {
    begin_operation("irx_arrow_table_combine");
    return set_error(
        IRX_ARROW_STATUS_NULL_POINTER,
        "out_failure must not be NULL");
  }
  *out_failure = nullptr;
  const irx_arrow_status status =
      irx_arrow_internal_table_combine(value, output);
  return finish_generated_call(
      status,
      "irx_arrow_table_combine",
      out_failure);
}

#endif
